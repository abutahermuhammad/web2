// Optional Chaining

// Ternary Operator
const isAdult = (age: number) => age === 18 ? true : false;

// Nullish Coalescing Operation
const isAuthenticated = null;
const result1 = isAuthenticated ?? 'guest';