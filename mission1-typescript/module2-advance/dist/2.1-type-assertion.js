"use strict";
{
    // Type Assertion/Type Narrowing
    let anything;
    anything = "Next Label Web Development";
    let anyTypeAsString = anything.toUpperCase();
    anything = 666;
    let anyTypeAsNumber = anything.toFixed(2);
    /**
     * Note:
     */
}
