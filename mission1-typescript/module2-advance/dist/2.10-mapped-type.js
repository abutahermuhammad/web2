"use strict";
{
    // Mapped type
    const arrayOfNumbers = [1, 4, 5];
    // Converting number array to string array
    const arrayOfStrings = arrayOfNumbers.map((number) => number.toString());
    console.log(arrayOfStrings);
    const area1 = {
        height: "100",
        width: 50
    };
}
